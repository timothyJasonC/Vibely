export type UserParams = {
    email: string,
    profilePhoto: string,
    username: string,
    title: string,
    description?: string,
    createdAt: string
}

export type TabType = "image" | "video" | "blog";
